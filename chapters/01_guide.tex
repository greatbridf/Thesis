\chapter{绪论与系统总体设计}\label{chap:introduction}

操作系统连接硬件资源与应用程序，其实现牵涉特权级、地址空间、异常中断、进程调度、文件接口和设备驱动等多个层面。在计算机科学与技术专业的本科教学中，进程、页表、系统调用等概念若缺少可运行内核的支撑，学生往往难以判断它们在真实执行路径中的相互依赖。代码规模适中、结构清楚、便于调试的教学操作系统，使抽象概念能够落到可观察的内核行为上，因而成为操作系统课程中重要的实验材料。

本课题以同济大学 Unix V6++ 教学操作系统为基础，研究其面向 RISC-V 平台的移植与 Rust 重构。重构前系统面向 IA-32 架构，内核主体采用 C++ 编写；本文在保留 Unix V6 风格系统调用、进程语义和 Shell 使用方式的前提下，重新实现启动、中断异常、系统调用、进程管理、设备适配、用户程序 ABI 和构建验证等路径。文件系统和内存管理在本文中作为系统运行的支撑条件处理，不作为主要展开对象。系统目标是在 QEMU virt 机器上启动到可交互 Shell，并运行常见用户程序、文件命令、进程和信号测试。

\section{研究背景与意义}

操作系统课程的难点，来自硬件机制和软件抽象在执行路径中的交叉。一次 Shell 命令可能经过用户程序装载、文件描述符、系统调用、Trap 返回和进程调度；一次终端输入也可能触发设备中断、TTY 行规程和信号处理。若只有概念说明而缺少可运行内核，学生很难把这些机制放回同一条控制流中理解。

经典教材通常从概念层面讨论进程管理、内存管理、文件系统、输入输出和安全机制等主题\cite{tanenbaum2017modern,arpaci2019ostep}，为理解操作系统原理提供了基本框架。本科阶段的系统实践还需要规模可控、便于阅读和修改的内核作为实验对象。Unix V6 及其教学化衍生系统保留了进程、文件、设备和系统调用等基本结构，又避免了现代通用操作系统的代码规模，因而适合作为观察内核内部关系的材料\cite{ritchie_unix_1974,lions_unix_1996}。

同济大学 Unix V6++ 是面向操作系统教学开发的 Unix V6 风格实验系统。该系统将早期 Unix 的核心思想迁移到更容易获得的硬件平台，并以面向对象方式重构了进程、文件系统、设备和中断等模块。它保留了小型 Unix 内核的主要组成部分，学生可以通过阅读和修改系统代码，观察系统调用如何进入内核、进程如何被调度、文件如何通过 inode 和缓冲区访问磁盘，以及终端输入如何驱动 Shell 交互。陈闳中教授关于 Unix V6++ 教学实践的回顾也明确提出了让学生动手修改、调试内核的课程目标\cite{chenhz_unixv6pp}。

RISC-V 开源指令集架构近年在教学、科研与产业中被持续采用。其非特权规范定义基础指令、寄存器和用户态执行模型，特权架构规范则对异常、中断、页表和特权级切换等操作系统所需机制作出了明确描述\cite{riscv_unprivileged_20191213,riscv_privileged_20211203}。与传统封闭指令集相比，RISC-V 的开放规范更便于在系统软件教学中展开硬件抽象层的说明，学生可以在学习指令集和特权级机制的同时，分析操作系统如何利用这些机制实现地址隔离、异常处理和设备访问。

将 Unix V6++ 移植到 RISC-V 平台，可以把原有课程中的 Unix V6++ 实验习惯与 RISC-V 开放规范放入同一套系统。移植过程需要重新处理启动流程、地址布局、中断控制器、设备模型、用户程序 ABI 和构建系统，也提供了一条从固件入口到 Shell 命令返回的观察路径。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\textwidth]{figures/research-positioning.pdf}
  \caption{本课题的研究定位与实现边界}\label{fig:research-positioning}
\end{figure}

\section{国内外研究现状}

教学操作系统的设计目标通常侧重在有限代码规模内覆盖核心机制，对工业级完备性的要求相对次要。Unix V6 是这一方向的重要源头。它以简洁的代码实现了进程、文件、目录、设备和系统调用等基本机制，长期被用作理解 Unix 操作系统结构的经典材料。以 Unix V6 为思想来源的教学系统强调可读性和可修改性，适合用来展示操作系统各模块之间的配合关系。

MIT 的 xv6 是当前国际上应用较广的 Unix-like 教学操作系统。xv6 早期版本面向 x86，后续版本迁移到 RISC-V，并在 MIT 操作系统课程中用于讲解进程、页表、锁、文件系统和系统调用等主题\cite{xv6_book_2023}。xv6 代码量较小，结构清晰，注释和实验材料也较完备，许多高校将其作为操作系统课程的重要参考。不过，xv6 更强调从 Unix V6 思想出发重写一个小型教学内核，而 Unix V6++ 则更突出在本校课程体系中长期积累的教学接口、实验习惯和用户态环境。

除 xv6 外，OS/161、Pintos 等系统也被广泛用于操作系统教学。OS/161 常用于配合同步、虚拟内存和文件系统实验，Pintos 则强调从半成品内核出发逐步完成调度、用户程序和文件系统等功能\cite{holland_os161_2002,pfaff_pintos_2009}。这类系统能够训练学生的内核编程能力，但其设计背景、运行环境和实验组织方式各不相同。本文选择依托已有 Unix V6++ 教学基础完成平台迁移，重点放在原有教学系统如何进入 RISC-V/Rust 环境。

国内 RISC-V 操作系统教学也已形成若干实践。rCore 教程以 Rust 语言和 RISC-V 架构为基础，从批处理系统、地址空间、进程管理到文件系统逐步构造操作系统内核\cite{rcore_tutorial_v3}。该项目表明，Rust 的类型系统和模块化组织可以用于教学内核，也为 RISC-V 系统软件实践提供了较完整的实验路径。QEMU 则为 RISC-V 提供系统级仿真环境，可模拟 virt 机器、串口、PLIC、VirtIO 设备等硬件组件，降低教学实验对真实硬件的依赖\cite{qemu_riscv_doc}。

Rust 语言近年来逐渐进入系统软件开发领域。Rust 通过所有权、生命周期和类型检查等机制，不依赖垃圾回收即可提供内存安全能力\cite{rust_reference}。已有 Rust 内核研究也表明，内核仍需保留少量受控的 unsafe 抽象，但类型系统可以帮助收束资源管理和隔离边界\cite{levy_kernel_rust_2017}。内核开发仍需处理裸机启动、外设访问、页表修改和上下文切换等底层操作，无法完全避免 unsafe 代码；但 Rust 的模块系统、枚举、模式匹配和 trait 有助于表达复杂状态和抽象边界。对于教学内核而言，Rust 保留了接近硬件的控制能力，也能减少空指针、悬垂引用和资源释放错误给代码阅读带来的额外负担。

\section{研究内容与边界}

本文围绕 Unix V6++ 的 RISC-V 移植与 Rust 重构展开。研究内容包括 RISC-V 启动入口、Sv39 地址空间、Trap 与系统调用、进程管理、ELF64/RISC-V 用户程序加载、信号机制、VirtIO 块设备、UART/TTY、Shell 交互、用户态构建链和 QEMU 验证流程。相较重构前的 \verb|main| 分支，当前系统的变化集中在体系结构、实现语言和运行环境三个方面：IA-32 转为 RISC-V，C++ 内核转为 Rust 内核，传统 PC 设备模型转为 QEMU virt、OpenSBI、PLIC、UART 和 VirtIO 组成的仿真平台。

这些内容并非简单替换编译目标。RISC-V 和 IA-32 在启动方式、中断入口、特权寄存器、页表格式、外设模型和用户态 ABI 上均存在差异；内核语言从 C++ 改为 Rust 后，进程对象、页资源、文件引用、inode 引用、中断保护和错误返回也需要重新表达。本文不把所有模块均等展开，而选择体系结构迁移中牵涉路径较长、接口边界较多的部分深入分析：第 2 章讨论 RISC-V 启动、Trap 和系统调用，第 3 章讨论进程生命周期、ELF 加载和信号，第 4 章专门比较重构前后的实现差异并归纳本次迁移的主要特点。文件系统和内存管理已有独立研究，本文只在这些路径需要时说明其接口作用。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\textwidth]{figures/work-scope-map.pdf}
  \caption{本文研究边界与重点展开对象}\label{fig:work-scope-map}
\end{figure}

\begin{table}[htbp]
  \centering
  \caption{本文研究内容与章节展开关系}\label{tab:work-scope}
  \renewcommand{\arraystretch}{1.32}
  \begin{tabular}{p{0.23\textwidth}p{0.43\textwidth}p{0.22\textwidth}}
    \toprule
    研究内容 & 当前系统中的结果 & 正文处理方式 \\
    \midrule
    RISC-V 平台基础 & OpenSBI 入口、Sv39 映射、PLIC、UART、VirtIO Block & 第 1、2 章作为基础说明 \\
    Trap 与系统调用 & 统一入口、上下文保存、系统调用参数传递和错误返回 & 第 2 章重点展开 \\
    进程与用户程序 & 调度、fork/exec/wait/exit、ELF64/RISC-V、信号处理 & 第 3 章重点展开 \\
    Rust 内核重构 & 所有权、生命周期、\texttt{Drop}、RAII 风格封装和错误类型 & 第 4 章集中比较 \\
    文件、设备与 Shell & 文件接口、TTY、Shell、管道和重定向验证 & 第 5 章作为运行闭环说明 \\
    构建与测试 & Cargo/Makefile、镜像生成、QEMU 交互测试 & 第 5 章集中呈现 \\
    \bottomrule
  \end{tabular}
\end{table}

\section{系统总体架构}

重构前的 Unix V6++ 面向 IA-32 平台，内核主体采用 C++ 语言实现。其整体结构延续 Unix V6 的设计思想，以进程、文件、inode、缓冲区、设备和系统调用为核心抽象，同时通过 C++ 类对各个模块进行封装。该版本能够在传统 PC 仿真环境中运行，并通过引导扇区、保护模式初始化、中断描述符表、页表、ATA 磁盘驱动和终端设备等机制完成一个小型 Unix-like 系统所需的基本功能。移植时，原系统的平台耦合集中体现在启动代码、中断控制器、块设备访问和用户程序格式上；若希望迁移到 RISC-V 教学平台，仅保留上层接口是不够的，必须重新设计体系结构相关层，并调整内核模块之间的边界。

重构后的系统以 RISC-V 64 位平台为目标，运行环境选用 QEMU virt 机器和 OpenSBI。内核主体采用 Rust 语言实现，用户态库、Shell 和常用用户程序仍使用 C 语言编写，并通过 RISC-V 交叉工具链编译为 ELF64/RISC-V 可执行文件。系统运行时由 QEMU 加载内核，内核完成初始化后创建初始进程，加载用户态 Shell，最终形成可交互的教学操作系统环境。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\textwidth]{figures/system-architecture.pdf}
  \caption{RISC-V/Rust 重构后系统分层结构}\label{fig:system-architecture}
\end{figure}

\begin{table}[htbp]
  \centering
  \caption{重构前后系统结构对比}\label{tab:system-before-after}
  \renewcommand{\arraystretch}{1.35}
  \begin{tabular}{p{0.2\textwidth}p{0.34\textwidth}p{0.34\textwidth}}
    \toprule
    比较维度 & 重构前系统 & 重构后系统 \\
    \midrule
    目标平台 & IA-32 传统 PC 平台 & RISC-V 64 位 QEMU virt 平台 \\
    内核语言 & C++ 为主，汇编用于启动和上下文切换 & Rust 为主，少量汇编用于 Trap 入口和上下文切换 \\
    启动方式 & 引导扇区加载内核，完成实模式到保护模式切换 & QEMU/OpenSBI 加载内核，进入 RISC-V Rust 入口 \\
    中断机制 & IDT、8259A、时钟与键盘/磁盘中断 & \texttt{stvec}、SBI timer、PLIC 和 UART 外部中断 \\
    块设备 & ATA/IDE 磁盘访问 & VirtIO Block 设备访问磁盘镜像 \\
    用户程序 & 原平台可执行文件与用户库 & ELF64/RISC-V 用户程序、C 用户库和 Shell \\
    构建方式 & CMake/Makefile 组织 C++ 内核与用户程序 & Cargo/Makefile 混合构建 Rust 内核、用户库和镜像 \\
    \bottomrule
  \end{tabular}
\end{table}

重构后系统可划分为启动与平台、地址空间与页资源、Trap 与系统调用、进程管理、文件设备与终端、用户态支持等部分。启动与平台代码处理 OpenSBI 入口、串口输出、平台常量和特权寄存器访问；地址空间与页资源代码维护 Sv39 页表、内核栈和用户空间；Trap 与系统调用代码保存上下文并分发时钟中断、外部中断和系统调用；进程管理代码负责进程控制块、调度、上下文切换、fork/exec/wait/exit 和信号处理；文件设备与终端代码向上提供 Unix V6 文件接口、VirtIO 块设备和 TTY；用户态支持代码把 C 用户库、Shell 和测试程序编译为 ELF64/RISC-V 程序，并通过系统调用与内核交互。

\section{本文组织结构}

全文共五章。第 1 章说明课题背景、相关教学操作系统、研究内容和系统总体架构；第 2 章讨论 RISC-V 启动、Trap、中断异常和系统调用；第 3 章讨论进程模型、调度、进程生命周期、ELF 加载和信号；第 4 章集中比较重构前后的实现差异，并说明 RISC-V 迁移和 Rust 重构带来的主要亮点；第 5 章给出文件设备交互环境、构建运行方式、测试用例、截图占位和结论展望。
